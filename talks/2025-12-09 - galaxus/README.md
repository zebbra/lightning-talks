# Galaxus Exporter

Simple script that exports all your ordered products from Galaxus into a CSV file.

## Usage

1. Copy the headers (especially cookies) from Chrome DevTools.
2. run `node export.js` in the terminal.
